import React from 'react'
import {css, jsx} from '@emotion/react'


const MusicPlayer  = () => (
    <div css={CSS}>
        
    </div>
)

const CSS = css `
    height:100%;
    width:100%;
    display:flex;
    position:relative;
    color:green;
`

export default MusicPlayer
